version: "3"
services:
  db:
    image: mariadb
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=ppshein123456
      - MYSQL_DATABASE=database_production
      - MYSQL_USER=root
      - MYSQL_PASSWORD=ppshein123456
    networks:
      - ufinity
  node:
    build: ./app
    depends_on:
      - db
    ports:
      - 3000:3000
    networks:
      - ufinity
networks:
  ufinity: